# Homelab SCCM - Environment Variable Configuration Example
#
# This file shows how to configure custom storage locations for the lab.
# Copy the relevant lines to your ~/.bashrc or ~/.zshrc
#
# DO NOT source this file directly - it's just documentation!
# These variables must be set BEFORE running `nix develop`

# ==============================================================================
# STORAGE CONFIGURATION
# ==============================================================================

# Vagrant Home - Where Vagrant stores downloaded box files
# Default: ~/.vagrant.d
# Disk usage: ~15-20GB for this lab
#
# To use custom location, add to ~/.bashrc or ~/.zshrc:
# export VAGRANT_HOME=/mnt/vms/vagrant-boxes

# VirtualBox Machine Folder - Where VirtualBox stores VM disk files
# Default: ~/VirtualBox VMs
# Disk usage: ~60-100GB per VM (~300GB total for 3 VMs)
#
# This is NOT an environment variable - configure with VBoxManage command:
# VBoxManage setproperty machinefolder /mnt/vms/virtualbox-vms

# ==============================================================================
# QUICK SETUP EXAMPLES
# ==============================================================================

# Example 1: Store everything in /mnt/vms
# -----------------------------------------
# Run the automated script (recommended):
#   ./scripts/configure-storage.sh /mnt/vms
#
# This will:
#   1. Create /mnt/vms/{vagrant-boxes,virtualbox-vms}
#   2. Add VAGRANT_HOME to your shell config
#   3. Configure VirtualBox machine folder
#   4. Verify the setup

# Example 2: Manual configuration for /mnt/vms
# ----------------------------------------------
# Add to ~/.bashrc or ~/.zshrc:
#   export VAGRANT_HOME=/mnt/vms/vagrant-boxes
#
# Configure VirtualBox (run once):
#   VBoxManage setproperty machinefolder /mnt/vms/virtualbox-vms
#
# Create directories:
#   mkdir -p /mnt/vms/{vagrant-boxes,virtualbox-vms}
#
# Reload your shell:
#   source ~/.bashrc  # or ~/.zshrc

# Example 3: Store on external drive
# ------------------------------------
# For external drive mounted at /media/myuser/external-drive:
#
# export VAGRANT_HOME=/media/myuser/external-drive/homelab/vagrant-boxes
# VBoxManage setproperty machinefolder /media/myuser/external-drive/homelab/virtualbox-vms

# Example 4: NixOS system-wide configuration
# -------------------------------------------
# Add to /etc/nixos/configuration.nix:
#
# environment.variables = {
#   VAGRANT_HOME = "/mnt/vms/vagrant-boxes";
# };
#
# systemd.tmpfiles.rules = [
#   "d /mnt/vms 0755 yourusername users -"
#   "d /mnt/vms/vagrant-boxes 0755 yourusername users -"
#   "d /mnt/vms/virtualbox-vms 0755 yourusername users -"
# ];
#
# Then run: sudo nixos-rebuild switch

# ==============================================================================
# VERIFICATION
# ==============================================================================

# After configuration, verify with:
#   echo $VAGRANT_HOME
#   VBoxManage list systemproperties | grep "Default machine folder"
#
# Then enter dev shell and check the output:
#   nix develop
#
# You should see "✓ Custom storage configured" if setup correctly.

# ==============================================================================
# DISK SPACE REQUIREMENTS
# ==============================================================================

# Expected disk usage:
#   - Base Vagrant boxes:        ~15-20GB (one-time download)
#   - DC01 VM (Domain Controller):  ~60GB
#   - SCCM01 VM (SCCM + SQL):      ~100GB
#   - CLIENT01 VM (Windows client): ~60GB
#   - Each additional client:       ~60GB
#
# Total for 3 VMs: ~300GB
# Add ~60GB per additional client VM

# Check available space before starting:
#   df -h /mnt/vms  # or wherever you're storing files
